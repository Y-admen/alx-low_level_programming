#include <stdio.h>
/**
*main -entry point
*describtion:
*Return: always 0 (success)
*/
int main(void)
{
	int num = 0;

	while (num <=  9)
	{
		printf("%i", num);
		num++;
	}
	printf("\n");
	return (0);
}
