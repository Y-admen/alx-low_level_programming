#include "main.h"

/**
 * _isdigit - function to check if digit
 *
 * @c: input parameter
 *
 * Return:1 for digite
 *	0 for not
*/
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	return (1);
	else
	return (0);
}


