#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

/***/

int main(int argc, char *argv[])
{
	int mul, num1, num2;

	if (argc < 3)
		return(printf("Error\n"), );
	if (!isdigit(num1) && !isdigit(num2)
		return (

